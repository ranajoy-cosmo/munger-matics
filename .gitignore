# macOS metadata
.DS_Store
**/.DS_Store

# MkDocs build output — never commit, deployed via GitHub Actions
site/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Test and analysis outputs
.coverage
.coverage.*
htmlcov/

# Virtual environments
.venv

# Environment variables
.env

# Local Claude Code instructions (personal/sensitive context)
CLAUDE.local.md

# Notebooks (exploration only — not version controlled)
notebooks/

# Data files (may contain sensitive financial data)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Notebooks — exploration only, not version controlled
notebooks/*
!notebooks/.gitkeep
